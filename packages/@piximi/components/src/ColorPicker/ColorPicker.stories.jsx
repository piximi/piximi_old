import React from 'react';

import { storiesOf } from '@storybook/react';

import { ColorPicker } from "..";

storiesOf('ColorPicker', module)
  .add('example', () => (
    <ColorPicker categories={} onChange={}/>
  ));
