import { createStyles } from '@material-ui/styles';

export const styles = () =>
  createStyles({
    container: {
      bottom: '16px',
      position: 'absolute',
      top: 0
    },
    canvas: {
      marginRight: '300px'
    }
  });
