import { createStyles } from '@material-ui/styles';

const styles = () =>
  createStyles({
    container: {
      bottom: '16px',
      position: 'absolute',
      top: 0
    }
  });

export default styles;
