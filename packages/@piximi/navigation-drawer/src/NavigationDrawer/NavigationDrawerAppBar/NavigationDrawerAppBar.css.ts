import {createStyles} from "@material-ui/core/styles";

export const styles = () =>
  createStyles({
    appBar: {
      position: "absolute"
    },
    "background-transparent": {
      background: "transparent"
    },
    "box-shadow-none": {
      boxShadow: "none"
    }
  });
